document.addEventListener('DOMContentLoaded',function(){function loadLeaderboardData(){fetch('leaderboard.csv').then(response=>response.text()).then(data=>{const rows=data.split('\n');const headers=rows[0].split(',');const tableBody=document.querySelector('.leaderboard-table tbody');tableBody.innerHTML='';for(let i=1;i<rows.length;i++){if(rows[i].trim()==='')continue;const cells=rows[i].split(',');const row=document.createElement('tr');for(let j=0;j<cells.length;j++){const cell=document.createElement('td');if(j===2){cell.textContent='₨ '+cells[j];}else{cell.textContent=cells[j];}row.appendChild(cell);}tableBody.appendChild(row);}}).catch(error=>{console.error('Error loading leaderboard data:',error);const tableBody=document.querySelector('.leaderboard-table tbody');tableBody.innerHTML='<tr><td colspan="3">Leaderboard data is currently unavailable. Please check back later.</td></tr>';});}loadLeaderboardData();});